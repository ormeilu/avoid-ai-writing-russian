## Что изменилось

## Проверки

- [ ] `uv run --group dev --group train pytest`
- [ ] `uv run --group dev --group train ty check`
- [ ] `uv run --group dev prek run --all-files` (в том числе самопроверка документации без находок P0 и P1)
- [ ] новое правило описано в каталоге (`skills/avoid-ai-writing-russian/references/`) и не срабатывает на `tests/fixtures/corpus/human/`
- [ ] запись в `CHANGELOG.md`, если изменение заметно пользователю
